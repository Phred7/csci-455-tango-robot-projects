
class IdRatherRipMyNailOFF:

    def __int__(self):
        self.currentCoords = self.initial_coords()
        self.nextCoords = (0,0)

    def initial_coords(self):
        return (0,0)


    #battle format??
    #robot has 400 hit points
    #depending on difficulty place in some # of enemies
    #battle win after all enemy die
    #enemy has hit chance of 50%, does 1 damage on hit

    #recharge format
    #go to node, get health set to 400

    #coffee format
    #go to node, get told nearest direction of exit

    #fun node
    #random effect of either health point, damage, told which node is exit, or nothing, instakill